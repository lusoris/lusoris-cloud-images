---
name: ci
"on":
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  PACKER_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  changes:
    name: Detect Changed Paths
    runs-on: ubuntu-latest
    outputs:
      packer: ${{ steps.filter.outputs.packer }}
      shell: ${{ steps.filter.outputs.shell }}
      go: ${{ steps.filter.outputs.go }}
      python: ${{ steps.filter.outputs.python }}
      docs: ${{ steps.filter.outputs.docs }}
      workflows: ${{ steps.filter.outputs.workflows }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36  # v3.0.2
        id: filter
        with:
          filters: |
            packer:
              - 'packer/**'
              - 'versions.json'
            shell:
              - 'packer/provisioners/*.sh'
              - 'scripts/*.sh'
            go:
              - 'go.mod'
              - 'go.sum'
              - 'cmd/**'
              - 'pkg/**'
            python:
              - 'tests/**'
              - 'pyproject.toml'
              - 'requirements-test.txt'
            docs:
              - 'docs/**'
              - 'mkdocs.yml'
              - 'README.md'
              - 'FLAVORS.md'
              - 'CHANGELOG.md'
            workflows:
              - '.github/workflows/**'
              - '.github/*.json'
              - '.yamllint.yml'
              - '.codespellrc'
              - 'Makefile'

  lint-packer:
    name: Packer Format & Validate
    needs: changes
    if: github.event_name == 'push' || needs.changes.outputs.packer == 'true' || needs.changes.outputs.workflows == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: hashicorp/setup-packer@1aa358be5cf73883762b302a3a03abd66e75b232  # v3.1.0
        with:
          version: "1.11.2"
      - name: Validate Packer
        run: |
          cd packer
          packer init .
          packer fmt -check .
          packer validate .

  lint-shell:
    name: ShellCheck & Shfmt Provisioners
    needs: changes
    if: github.event_name == 'push' || needs.changes.outputs.shell == 'true' || needs.changes.outputs.workflows == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Install Shell Analyzers
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck shfmt
      - name: Run ShellCheck
        run: shellcheck packer/provisioners/*.sh scripts/*.sh
      - name: Check Formatting (shfmt)
        run: shfmt -d -i 2 -ci packer/provisioners/*.sh scripts/*.sh

  lint-workflows:
    name: Actionlint & Yamllint
    needs: changes
    if: github.event_name == 'push' || needs.changes.outputs.workflows == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - name: Install Workflow Linters
        run: |
          sudo apt-get update
          sudo apt-get install -y yamllint
          bash -c "$(curl -sSfL https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash)"
          sudo mv ./actionlint /usr/local/bin/
          pip install codespell
      - name: Run Actionlint
        run: actionlint .github/workflows/*.yml
      - name: Run Yamllint
        run: yamllint -c .yamllint.yml .github/ packer/http/
      - name: Run Codespell
        run: codespell

  test-go:
    name: Go 1.27 Build & Unit Tests
    needs: changes
    if: github.event_name == 'push' || needs.changes.outputs.go == 'true' || needs.changes.outputs.workflows == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: actions/setup-go@f111f3307d8850f501ac008e886eec1fd1932a34  # v5.3.0
        with:
          go-version: "1.27.x"
          check-latest: true
      - name: Run Go Unit Tests
        run: go test ./... -v
      - name: Compile lusoris-forge
        run: go build -o bin/lusoris-forge ./cmd/lusoris-forge

  test-python:
    name: Pytest Verification Suite
    needs: changes
    if: >-
      github.event_name == 'push' ||
      needs.changes.outputs.python == 'true' ||
      needs.changes.outputs.packer == 'true' ||
      needs.changes.outputs.shell == 'true' ||
      needs.changes.outputs.workflows == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38  # v5.4.0
        with:
          python-version: "3.12"
      - uses: hashicorp/setup-packer@1aa358be5cf73883762b302a3a03abd66e75b232  # v3.1.0
        with:
          version: "1.11.2"
      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck
          cd packer && packer init . && cd ..
          pip install -r requirements-test.txt
      - name: Run Pytest Suite
        run: pytest tests/ -v

  docs-strict:
    name: Strict MkDocs Build
    needs: changes
    if: github.event_name == 'push' || needs.changes.outputs.docs == 'true' || needs.changes.outputs.workflows == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38  # v5.4.0
        with:
          python-version: "3.12"
      - name: Install MkDocs Dependencies
        run: pip install mkdocs-material mkdocs-minify-plugin
      - name: Build MkDocs Strict
        run: mkdocs build --strict

  gitleaks:
    name: Secret & Privacy Leak Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7  # v2.3.9
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
