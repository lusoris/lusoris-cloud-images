title = "lusoris-cloud-images gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Project-specific false positives"
paths = [
  '''^\.git/''',
  '''^output-images/''',
  '''^packer/packer_cache/''',
  '''^versions\.json$''',
]

stopwords = [
  "pve.example.com",
  "192.0.2.",
  "placeholder-secret",
  "example.com",
]

[[rules]]
id = "no-cosign-private-key"
description = "A cosign private key must never enter the repo (we use keyless OIDC)."
regex = '''-----BEGIN ENCRYPTED COSIGN PRIVATE KEY-----'''
tags = ["key", "cosign"]
